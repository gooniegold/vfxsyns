"use client";

/**
 * Lenis smooth scroll was preventing reliable native document scrolling in this layout.
 * Native scroll only until reconfigured with an explicit wrapper/content target.
 */
export function SmoothScroll() {
  return null;
}
